import { Box, Card, Text } from "@chakra-ui/react";

function FollowBar() {
  return (
    <Box px={3}>
      <Card mx={1} p={2} h={500}>
        <Text>FollowBar</Text>
      </Card>
    </Box>
  );
}
export default FollowBar;
